name: CI workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@6

      - name: Use Node.js 22
        uses: actions/setup-node@4
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npm run build
